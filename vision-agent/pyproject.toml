[project]
name = "duolingo-ai-teacher"
version = "0.1.0"
description = "AI language teacher agent powered by OpenAI Realtime + Stream Edge"
requires-python = ">=3.12,<3.14"
dependencies = [
    "vision-agents[getstream,openai]>=0.1.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
